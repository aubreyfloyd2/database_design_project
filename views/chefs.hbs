{{!-- // 1 # Citation for the following page:
// 2 # Date: 11/20/2023
// 3 # Adapted from code
// 4 # Much of the format is from the node starter guide. Variables and table values original
// 5 # Source URL: https://github.com/osu-cs340-ecampus/nodejs-starter-app --}}

<h2>Chefs</h2>
{{!-- Create a table --}}
<table id="chefs-table">
    {{!-- Header section --}}
    <thead>

        {{!-- For just the first row, we print each key of the row object as a header cell so we
        know what each column means when the page renders --}}
        <tr>
            <th>Chef ID</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Location</th>
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row header in order --}}
        {{#each data}}
        <tr>
            <td>{{this.chef_id}}</td>
            <td>{{this.first_name}}</td>
            <td>{{this.last_name}}</td>
            <td>{{this.email}}</td>
            <td>{{this.location}}</td>
            <td><button onclick="deleteChef({{this.chef_id}})">Delete</button></td>
        </tr>
        {{/each}}
    </tbody>
</table>
<script src="./js/delete_chef.js"></script>

<h2>Add a Chef</h2>
<p>To add a new chef, please enter their information below and click 'Submit'!</p>
<form id="add-chef-form" method="POST" action="/add-chef-form">
   
    <label for="input-first-name">First Name: </label>
    <input type="text" name="input-first-name" id="input-first-name">

    <label for="input-last-name">Last Name: </label>
    <input type="text" name="input-last-name" id="input-last-name">

    <label for="input-email">Email: </label>
    <input type="text" name="input-email" id="input-email">


    <label for="input-chef-location">Chef Location: </label>
    <select name="input-chef-location" id="input-chef-location">
        <option value="test">Select a Location</option>
        {{#each restaurants}}
        <option value="{{this.restaurant_id}}">{{this.location}}</option>
    {{/each}}

    <input type="submit">
</form>


<h2>Search for a Chef</h2>
<p>Search for a Chef by last name using the field below. Partial names are OK too!</p>
<form id="search-chef-html" method='GET' action='/chefs'>
    <label for="input-last-name">Search by Chef Name: </label>
    <input type="text" name="last_name" id="input-last-name-html-search">

    <input type="submit">
    <a href='/chefs'><button>Reset</button></a>
</form>

<h2>Update Chef Information</h2>
<p>To update a specific Chef, select the name from the drop-down menu and input their new information.</p>
<form id="update-chef-form-ajax">

    <label for="mySelect">Chef: </label>
    <select name="input-location" id="mySelect">
        <option value="test">Select a Chef</option>
        {{#each data}}
        <option value="{{this.chef_id}}">{{this.first_name}} {{this.last_name}}</option>
        {{/each}}
    </select>

    <label for="input-first-name-update">First Name: </label>
    <input type="text" name="input-first-name-update" id="input-first-name-update">

    <label for="input-last-name-update">Last Name: </label>
    <input type="text" name="input-last-name-update" id="input-last-name-update">

    <label for="input-email-update">Email: </label>
    <input type="text" name="input-email-update" id="input-email-update">

    <label for="input-chef-location-update">Chef Location: </label>
    <select name="input-chef-location-update" id="input-chef-location-update">
        <option value="test">Select a Location</option>
        {{#each restaurants}}
        <option value="{{this.restaurant_id}}">{{this.location}}</option>
    {{/each}}
    </select>


    <input type="submit">

</form>

<script src="./js/update_chef.js"></script>